import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['www.freeiconspng.com'], // add any external domains you use
  },

};

export default nextConfig;
